{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"title": "Application settings mapping",
	"type": "object",
	"properties": {
		"paths": {
			"type": "array",
			"description": "Contains a list of resource mappings.",
			"items": {
				"type": "object",
				"properties": {
					"resourceName": {
						"description": "The name of the file or directory located in the `app-settings` folder.",
						"type": "string"
					},
					"destination": {
						"description": "The destination path. It must be an absolute path.",
						"type": "string"
					}
				}
			}
		}
	},
	"required": ["paths"]
}
